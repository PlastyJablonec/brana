rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ EMERGENCY ONLY - COMPLETELY OPEN RULES
    // POUŽIJ JEN PRO TESTOVÁNÍ!
    
    match /{document=**} {
      // Povolit VŠEM VŠECHNO (i nepřihlášeným!)
      allow read, write: if true;
    }
  }
}